using UnityEngine;
using System.Collections;

public class spell_collision_manager : MonoBehaviour {

	void OnTriggerEnter2D(Collider2D coll){
		//Debug.Log (this);
		//GameObject.Find ("spell_manager").GetComponent<spell_manager> ().remove_spell(this.gameobject);
		//GameObject.Destroy(this.gameObject);
	}

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {

	}
}
